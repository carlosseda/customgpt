Abrir la base de datos de vectores. En la carpeta del proyecto:

```
chroma run --path ./api/src/chromadb
```

Para selenium, instalar el último drive de Chrome y verificar que nuestro navegador está en la misma versión

```
https://googlechromelabs.github.io/chrome-for-testing/#stable
```
